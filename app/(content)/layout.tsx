const ContentLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <div>
      {/* TODO Add Navigation Side Menu */}
      {children}
    </div>
  );
};

export default ContentLayout;
